{
  "name": "{{name}}",
  "version": "0.0.0",
  "description": "{{description}}",
  "author": "Alec Larson",
  "license": "MIT",
  "repository": "{{org}}/{{name}}",
  "main": "dist/{{name}}.js",
  "module": "dist/{{name}}.mjs",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "rollup -c",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "@alloc/prettier-config": "^1.0.0",
    "esbuild": "^0.8.51",
    "esbuild-jest": "npm:@alloc/esbuild-jest",
    "jest": "^26.6.3",
    "prettier": "^2.0.5",
    "react": "^17.0.1",
    "rollup": "^2.39.1",
    "rollup-plugin-dts": "^2.0.1",
    "rollup-plugin-esbuild": "^2.6.1",
    "typescript": "^4.0.0"
  },
  "prettier": "@alloc/prettier-config",
  "keywords": [
    "react"
  ]
}
